import React, { useState } from 'react';

const UiClass = () => {
   
  

  // useEffect(() => {
  //   fetch(`http://localhost:5000/courses/${_id}`)
  //     .then(response => response.json())
  //     .then(data => {
  //       console.log('Fetched course data:', data); // Check if data is fetched correctly
  //       setCourseData(data);
  //     })
  //     .catch(error => console.error('Error fetching course details:', error));
  // }, [_id]);

  // console.log('Course ID:', _id); // Check the value of the ID received from useParams


  

  return (
    <div className="shadow-2xl h-[350px] rounded-2xl">
      Coming soon
    </div>
  );
};

export default UiClass;
